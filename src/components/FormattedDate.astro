---
interface Props {
  date: string;
}

const { date } = Astro.props;
import { format } from "date-fns-tz";
---

<time>
  {
    format(date, "yyyy年MM月dd日", {
      timeZone: "Asia/Tokyo",
    })
  }
</time>
